Updated the existing `<p>` content to the exact required text without touching any other HTML, per `SPEC.md` in `index.html`.

- Change made in `index.html`
- Tests not run (no test command or harness found in the repo)

Changelog:
- Set the existing `<p>` text to `Hello world` in `index.html`.