Updated the existing paragraph text to match the spec’s acceptance criteria, changing only the content inside the `<p>` tag in `index.html`.

Tests not run (no test or build commands are defined in this repo).

Changelog:
- Updated paragraph text in `index.html` to `Hello world`.